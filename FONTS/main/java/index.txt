Archivos fuente del proyecto, separados segun la capa en la que se encuentran, siendo estas:
- Presentacion
- Dominio
- Persistencia (no implementada esta primera entrega)

Se añade ademas la carpeta con excepciones que heredan de la clase Exception de Java, y que se utilizan para el correcto funcionamiento del programa.

El codigo mediante el driver puede ser ejecutado y probado desde la carpeta EXE con los comandos descritos alli.